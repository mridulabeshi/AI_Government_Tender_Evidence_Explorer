duckdb>=1.0.0
pandas>=2.0.0
pdfplumber>=0.11.0
pymupdf>=1.24.0
streamlit>=1.35.0
plotly>=5.20.0
anthropic>=0.30.0
pyarrow>=15.0.0
requests>=2.31.0

# Optional — only needed if you enable OCR fallback for scanned PDFs:
# pytesseract>=0.3.10
# pillow>=10.0.0
# (also requires the system package: tesseract-ocr)
