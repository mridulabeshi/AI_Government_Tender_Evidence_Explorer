# Generated data artifacts (Parquet/SQLite) — regenerated by running the
# pipeline scripts locally or on first deploy. Commit a populated data/
# folder instead if you want the dashboard to show results immediately
# on a fresh deploy (see README).
data/
__pycache__/
*.pyc
.venv/
venv/
.streamlit/secrets.toml
